#Display wrong if the condition isn't met.
if "dog" == "cat":
  print ("That's crazy")
# On the next two lines, display "wrong" if the condition isn't met.
else:
  print("wrong")
  
  #The Reult is (wrong).
  
 #part2 If the first condition isn't met, test whether "cat" equals "cat." If so display ok.
 
 if "dog" == "cat":
  proposition = "crazy"
else:
  "cat" == "cat"
  print("ok")

#The Reult is (ok).
